% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apv.R
\name{apv_helpers}
\alias{apv_helpers}
\alias{.apv_setup}
\alias{.apv_moments}
\title{Set up and evaluate the APV of a loan (internal use)}
\usage{
.apv_setup(
  x,
  cur_age,
  orig_term,
  orig_loan_amt,
  mon_pmt,
  recov_curve,
  orig_apy,
  type
)

.apv_moments(setup, rate)
}
\arguments{
\item{x}{An object of class \code{alife_multi}. Typically the output of the
\code{estimate_hazard} function with two event types.}

\item{cur_age}{Current age of the loan in months.}

\item{orig_term}{Original loan term in months.}

\item{orig_loan_amt}{Original loan amount.}

\item{mon_pmt}{Monthly payment amount.}

\item{recov_curve}{Recovery curve \code{data.frame} with columns \code{month} and
\code{recovery} (the recovery rate as a proportion of \code{orig_loan_amt}). It must
contain every month from \code{cur_age} to \code{orig_term - 1}.}

\item{orig_apy}{Internal loan APY for amortization. Defaults to 0.15.}

\item{type}{A character string specifying the extrapolation type passed to
\code{\link[=extend_hazard]{extend_hazard()}}: either \code{"constant"} (default), \code{"geometric"} or
\code{"linear"}.}

\item{setup}{The output of \code{.apv_setup()}.}

\item{rate}{A monthly discount rate.}
}
\value{
\code{.apv_setup()} returns a list with the number of remaining months,
the default and prepayment probabilities, and the cash flows for each
remaining month. \code{.apv_moments()} returns the same list as
\code{\link[=calculate_apv]{calculate_apv()}}.
}
\description{
\code{.apv_setup()} checks the inputs of \code{\link[=calculate_apv]{calculate_apv()}} and
\code{\link[=solve_irr]{solve_irr()}} and computes everything that does not depend on the
discount rate (event probabilities and cash flows). \code{.apv_moments()}
computes the APV, its second moment, and its standard deviation for a
given monthly discount rate.
}
\keyword{internal}
