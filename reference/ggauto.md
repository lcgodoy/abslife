# Plot an 'alife' (or `multi_alife`) Object using `ggplot2`

Plot an 'alife' (or `multi_alife`) Object using `ggplot2`

## Usage

``` r
ggauto(object, ...)
```

## Arguments

- object:

  An object of class `alife` or `alife-multi`.

- ...:

  Not used.

## Value

ggplot.
